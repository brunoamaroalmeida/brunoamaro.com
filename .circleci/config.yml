version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: Install Hugo
          command: apt-get update && apt-get install hugo -qy


      - run:
          name: Generate Static Content
          command: hugo -s

      - run:
          name: List
          command: ls -lh      
